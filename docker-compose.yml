# Optional Docker deployment
# Run: docker-compose up -d
version: "3"
services:
  orchestrator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    # Config from config/settings.yaml (run setup first)
    restart: unless-stopped
